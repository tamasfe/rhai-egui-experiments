egui::layout::central_panel(egui_ctx, |ui| {
    ui.heading(`frame: ${data.frame_time_ns()}`);
    ui.heading(`frame: ${data.frame_time_ns()}`);
    ui.heading(`frame: ${data.frame_time_ns()}`);
    ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
    // ui.heading(`frame: ${data.frame_time_ns()}`);
});
